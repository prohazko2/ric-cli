export const command = "send";
export const description = "Send commands to device";

export function builder(yargs) {
  return yargs
    //.command(require("./00-health"))
    //.command(require("./01-models"))
   // .command(require("./02-objects"));
}
